version: 0.0
os: linux
files:
- source: /
  destination: /home/ubuntu/node

hooks:
AfterInstall:
  - location: /bin/install-pkgs
    timeout: 120
    runas: root
ApplicationStart:
  - location: /bin/app-start
    timeout: 60
    runas: root
 
